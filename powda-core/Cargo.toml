[package]
name = "powda-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
async-trait = "0.1.89"
# thiserror = "2.0.17"
# chrono = {version = "0.4", features=["serde"]}
# zeroize = "1.6"

